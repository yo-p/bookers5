<p id="notice">
  <%= notice %>
</p>

<h1>welcome to Bookers !!</h1>

<% if user_signed_in? %>
  <div class="row"><%= link_to "Sign Up", user_path(current_user) %></div>
  <div class="row"><%= link_to "Log in", user_path(current_user) %></div>
<% else %>
  <div class="row"><%= link_to "Sign Up", new_user_registration_path%></div>
  <div class="row"><%= link_to "Log in", new_user_session_path%></div>
<% end %>

